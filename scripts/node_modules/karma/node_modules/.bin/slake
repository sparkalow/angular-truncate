#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../LiveScript/bin/slake" "$@"
  ret=$?
else 
  node  "$basedir/../LiveScript/bin/slake" "$@"
  ret=$?
fi
exit $ret
